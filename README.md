# S1000D Mini-CSDB v1.1

A browser-based educational CSDB emulator built around the S1000D Issue 6 R2 Bike sample data.

## What it demonstrates

- Browse real S1000D Bike Data Modules and other objects
- Search by DMC, title, filename and schema
- Inspect metadata, readable content and source XML
- Follow `dmRef` links and Publication Module references
- Preview browser-compatible ICN illustrations referenced by a module
- See referenced ICNs that require formats such as CGM even when no browser preview is available
- Run a small set of **BREX-informed checks** read from the actual Bike BREX Data Module
- Simulate Check out → In Work → Review → Approval → Issued
- Keep an audit trail in browser `localStorage`
- Import another extracted S1000D XML folder locally

## Important scope note

This is an educational emulator, not a production CSDB, a full BREX engine, or an S1000D conformance validator. Workflow edits are local simulations and do not modify source XML.

## Run

Open `index.html` in a modern browser. No server or build process is required.


## v1.1 authoring workflow
A checked-out Data Module is now opened in a separate **Authoring Editor** simulation rather than being edited directly in the CSDB view. The CSDB retains repository/workflow responsibility, while the editor view represents a dedicated XML authoring tool. Changes are stored only in browser localStorage; the bundled Bike XML is not modified.


## v1.1 additions
- Dashboard with managed-object and workflow counts plus recent changes.
- Data Module / Publication Module **Where used** impact view.
- **Issue history** showing the source Bike issue and simulated releases generated by approval workflow.


## v1.1
- Dashboard moved from the right-hand detail pane into the central workspace.
- Dashboard uses the full content area while active; selecting an object restores the standard three-pane CSDB layout.
- No feature changes from v0.6.


## v1.1
- Structured procedural content rendering from `mainProcedure` / `proceduralStep` XML.
- Resolves local `internalRef` targets such as support equipment and supplies.
- Preserves random/sequential lists without duplicate flattened text.
- Keeps all v0.7 CSDB, workflow, ICN, BREX, where-used and issue-history features.


## v1.1
The simulated external authoring editor now uses a structured, editable Author view. Semantic hierarchy controls typography: body text remains regular, headings/parent steps are semibold, lists preserve hierarchy, and internal references and alerts retain their visual treatment.


## Publication Module lab
v2.14 adds hierarchical PM structure, assembled publication preview, and local/simulated Publication Module creation stored in browser localStorage.


## Internal references
v2.14 makes rendered S1000D internal references interactive. Clicking a reference opens a small local reference card with ID/type and resolved record details where available.


## Internal reference fix
v2.14 preserves internalRefId in rendered HTML and uses delegated click/keyboard handling so references remain interactive after navigation and re-rendering.


## Illustration references
v2.14 resolves figure internal references to their ICN. Browser-viewable assets are shown inline; CGM source illustrations are identified explicitly and can be opened in the ICN Library.


## Navigation fix
v2.14 fixes stale ICN-library rows being rendered in the object table when navigating back to a Data Module.


## ICN Where used
v2.14 adds a focused Where-used view for ICNs. The centre list now shows only Data Modules that reference the selected illustration, with a clear count and Back to ICN Library action.


## ICN Where-used navigation
v2.14 keeps the centre column focused on only the Data Modules that use an ICN when navigating from that ICN's Where-used list.


## Startup detail fix
v2.14 restores the default Brake system — Manual test detail pane on initial page load while keeping ICN Where-used focus navigation intact.


## Media navigation
v2.17 makes ICN codes in a Data Module's Media tab clickable, opening the matching illustration directly in ICN Library.


## Media-to-ICN navigation fix
v2.17 patches the actual Media-card renderer so the visible ICN code is a real navigation button to ICN Library.


## ICN Library view reset
v2.22 rebuilds the ICN Library table whenever it is entered, preventing stale Where-used headers from remaining after Media-to-ICN navigation.


## ICN state cleanup
v2.22 removes the obsolete v2.12 ICN Where-used branch from apply(), leaving a single source of truth for ICN Library rendering.


## ICN detail sync
v2.22 ensures ICN detail rendering takes precedence whenever the app is in ICN Library mode, preventing a previously selected DM detail pane from remaining visible.


## Root-cause navigation fix
v2.22 restores the missing renderCount() function. Its absence caused a ReferenceError after centre-pane rendering, preventing detail-pane updates during startup and ICN navigation.
